AM_CXXFLAGS =               \
        $(SDL_CFLAGS)       \
        $(SDL_image_CFLAGS) \
        $(opengl_CFLAGS)    \
        $(glew_CFLAGS)      \
        $(glm_CFLAGS)       

bin_PROGRAMS = silverwaffle

silverwaffle_LDADD =       \
        $(SDL_LIBS)        \
        $(SDL_image_LIBS)  \
        $(opengl_LIBS)     \
        $(glew_LIBS)       

silverwaffle_SOURCES = main.cc
